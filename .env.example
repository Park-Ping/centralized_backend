PORT=3000
# Prefer specifying the db name explicitly (e.g. /parkping) to avoid defaulting to "test".
# If your environment has issues with SRV DNS lookups, use a standard seedlist connection string (mongodb://host1,host2,host3/...).
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER_HOSTNAME/parkping?appName=ParkPing-Cluster

# Exposed ngrok URL for backend callbacks/webhooks
BACKEND_PUBLIC_URL=https://demarcus-acidulous-amy.ngrok-free.dev

# Frontend URLs for CORS
USER_FRONTEND_URL=http://localhost:4028
ADMIN_FRONTEND_URL=http://localhost:4029

# Optional comma-separated extra origins
CORS_ORIGINS=http://127.0.0.1:4028,http://127.0.0.1:4029

# PhonePe sandbox
PHONEPE_MERCHANT_ID=M23FAFMG3VQ22_2602091510
PHONEPE_SALT_KEY=NjBiNDViNjgtNmUxMC00YjdmLWFhMDQtNzE1ZjkyOTQwMGI2
PHONEPE_SALT_INDEX=1
PHONEPE_PAY_API_URL=https://api-preprod.phonepe.com/apis/pg-sandbox/pg/v1/pay
PHONEPE_REDIRECT_MODE=REDIRECT

# Dev only: bypass PhonePe and auto-mark payment SUCCESS
PHONEPE_BYPASS=false

# Admin auth (JWT)
ADMIN_JWT_SECRET=change-me

# SMTP (required for secure admin forgot-password OTP flow)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password
SMTP_FROM=ParkPing Admin <your-email@example.com>
